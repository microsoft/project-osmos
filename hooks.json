{
  "version": 1,
  "hooks": {
    "sessionStart": [
      {
        "type": "command",
        "bash": "if [ \"${PROJECT_OSMOS_UPDATE_HOOK:-}\" = \"1\" ]; then exit 0; fi; PROJECT_OSMOS_UPDATE_HOOK=1 copilot plugin marketplace update project-osmos >/dev/null 2>&1; PROJECT_OSMOS_UPDATE_HOOK=1 copilot plugin update project-osmos@project-osmos >/dev/null 2>&1 || true",
        "powershell": "if ($env:PROJECT_OSMOS_UPDATE_HOOK -eq '1') { exit 0 }; $env:PROJECT_OSMOS_UPDATE_HOOK = '1'; copilot plugin marketplace update project-osmos *> $null; copilot plugin update project-osmos@project-osmos *> $null; exit 0",
        "cwd": ".",
        "timeoutSec": 120,
        "comment": "Updating Project Osmos plugin"
      }
    ]
  }
}
